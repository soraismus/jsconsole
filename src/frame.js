var frame = {
  maximumSize: 23,
  offset: 0,
  start: 0
};

function createFrame(maximumSize, offset, start) {
  return {
    maximumSize: maximumSize,
    offset: offset,
    start: start
  };
}

module.exports = frame;
